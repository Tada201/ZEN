// The chart.js-based ChartBlock was retired — chart rendering lives in the
// canonical recharts surface (src/atlas/components/chat/ChartBlock.tsx via
// the shared src/lib/chart.ts module). Keep the barrel for remaining widgets.
export { GlobeWidget } from './GlobeWidget';