sed -i 's/pub async fn provider(&self) -> ZenResult<Arc<dyn LlmProvider>> {/pub async fn provider(\&self) -> ZenResult<Arc<dyn LlmProvider>> {\n        let db = self.db().await?;\n        let active_provider = crate::db::queries::get_setting(\&db, "active_provider")\n            .await\n            .unwrap_or_default()\n            .unwrap_or_else(|| "ollama".to_string());\n        Ok(crate::llm::create_provider(\&db, \&active_provider).await)/' D:/DATA_VOLUME_D/VScode/GG_ANTIGRAV/ZEN/src-tauri/src/commands/mod.rs
sed -i 's/self.llm.get().await.map(|l| l.clone())//' D:/DATA_VOLUME_D/VScode/GG_ANTIGRAV/ZEN/src-tauri/src/commands/mod.rs
